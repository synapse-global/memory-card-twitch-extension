const ORIGIN = "https://api.memorymatch.info";
const VERSION = "/api/v1";
const URL = `${ORIGIN}${VERSION}`;

const ROUTES = {
  combined: `${URL}/combined`,
  viewer: `${URL}/viewer`,
  
};

export {
  ORIGIN,
  VERSION,
  URL,
  ROUTES
}